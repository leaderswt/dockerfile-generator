# 🐳 Dockerfile生成器 - 快速参考卡

## 🚀 30秒快速开始

### Windows用户
```
1. 双击"启动.bat"
2. 等待自动打开浏览器
3. 开始使用！
```

### 其他系统
```bash
pip install -r requirements.txt
python app.py
# 访问 http://127.0.0.1:5000
```

---

## 🎯 常用命令

### 构建镜像
```bash
docker build -t myapp .
```

### 运行容器
```bash
docker run -p 8080:5000 myapp
```

### Docker Compose
```bash
docker-compose up -d
```

### 查看日志
```bash
docker logs <container_id>
```

### 进入容器
```bash
docker exec -it <container_id> /bin/bash
```

---

## 📦 推荐镜像

| 语言 | slim镜像 | 说明 |
|------|---------|------|
| Python | `python:3.11-slim` | 150MB |
| Node.js | `node:18-alpine` | 130MB |
| Java | `openjdk:17-slim` | 200MB |
| Go | `golang:1.21-alpine` | 250MB |
| PHP | `php:8.2-apache` | 400MB |
| Nginx | `nginx:alpine` | 25MB |

---

## ⚡ 快速模板

### Python Flask
```dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
EXPOSE 5000
CMD ["python", "app.py"]
```

### Node.js Express
```dockerfile
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
EXPOSE 3000
CMD ["npm", "start"]
```

### Go Gin
```dockerfile
FROM golang:1.21-alpine
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN go build -o main .
EXPOSE 8080
CMD ["./main"]
```

### React (多阶段)
```dockerfile
FROM node:18-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build

FROM nginx:alpine
COPY --from=builder /app/dist /usr/share/nginx/html
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

---

## 🔧 .dockerignore示例

```
# 依赖
node_modules
__pycache__
venv
.env

# Git
.git
.gitignore

# IDE
.vscode
.idea
*.swp

# 测试
*.test.js
*.spec.js
test/
coverage/

# 日志
*.log
logs/

# 其他
.DS_Store
Thumbs.db
docker-compose.yml
Dockerfile
README.md
```

---

## 💡 端口对照表

| 框架 | 默认端口 |
|------|---------|
| Flask | 5000 |
| Django | 8000 |
| Express | 3000 |
| Next.js | 3000 |
| Spring Boot | 8080 |
| Gin | 8080 |
| Laravel | 80 |
| React (Vite) | 5173 |
| Vue (Vite) | 5173 |

---

## 🛡️ 安全检查清单

- [ ] 使用非root用户
- [ ] 基础镜像有版本号
- [ ] .dockerignore已配置
- [ ] 无硬编码密码
- [ ] 端口正确暴露
- [ ] 有健康检查
- [ ] 多阶段构建（编译型语言）

---

## 📚 学习路径

### 第一天：基础
1. 选择一个预设模板
2. 生成第一个Dockerfile
3. 构建并运行容器
4. 熟悉基本命令

### 第三天：进阶
1. 自定义配置
2. 添加环境变量
3. 配置健康检查
4. 使用.dockerignore

### 第七天：精通
1. 多阶段构建
2. Docker Compose编排
3. 性能优化
4. 安全加固

---

## 🔍 调试技巧

### 查看容器进程
```bash
docker ps
```

### 检查容器配置
```bash
docker inspect <container_id>
```

### 查看资源使用
```bash
docker stats
```

### 检查网络
```bash
docker network ls
docker network inspect <network>
```

---

## 📞 常用端口

| 端口 | 服务 |
|------|------|
| 80/443 | Web服务器 |
| 3306 | MySQL |
| 5432 | PostgreSQL |
| 6379 | Redis |
| 27017 | MongoDB |
| 9200 | Elasticsearch |

---

## ⚠️ 常见错误

| 错误 | 原因 | 解决 |
|------|------|------|
| `port is already allocated` | 端口占用 | 换一个端口 |
| `no such file or directory` | 路径错误 | 检查COPY路径 |
| `permission denied` | 权限问题 | 检查文件权限 |
| `connection refused` | 服务未启动 | 检查启动命令 |

---

**保存此文件作为快速参考！**
