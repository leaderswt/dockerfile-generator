# 🐳 Dockerfile生成器 - 快速使用指南

## 🚀 立即开始

### 第一步：启动应用

#### Windows用户
1. 双击运行 `启动.bat`
2. 等待自动安装依赖（如需要）
3. 浏览器自动打开 http://127.0.0.1:5000

#### Mac/Linux用户
```bash
chmod +x 启动.sh
./启动.sh
```

#### 手动启动
```bash
# 安装依赖
pip install -r requirements.txt

# 启动应用
python app.py
```

### 第二步：使用工具

#### 方法一：使用预设模板（推荐新手）

1. **选择技术栈**
   - 在首页看到10个预设模板卡片
   - 选择与你项目最匹配的（如 Python Flask、Node.js Express）
   - 点击卡片即可自动填充配置

2. **生成Dockerfile**
   - 点击蓝色大按钮"🎯 生成 Dockerfile"
   - 右侧预览区会立即显示生成的Dockerfile

3. **获取结果**
   - 📋 **复制**：一键复制到剪贴板
   - ⬇️ **下载**：下载Dockerfile文件
   - 📥 **.dockerignore**：下载.dockerignore文件

#### 方法二：自定义配置

1. **选择基础镜像**
   - 根据你的编程语言选择：
     - Python项目 → `python:3.11-slim`
     - Node.js项目 → `node:18-alpine`
     - Java项目 → `openjdk:17-slim`
     - Go项目 → `golang:1.21-alpine`

2. **设置工作目录**
   - 默认 `/app`，通常不需要修改

3. **配置端口**
   - 填写应用运行的端口号
   - 如：Flask用5000，Django用8000，Express用3000

4. **选择包管理器**
   - Python → pip（需要requirements.txt）
   - Node.js → npm（需要package.json）
   - Java → Maven

5. **添加环境变量**（可选）
   - 点击"+ 添加环境变量"
   - 填写变量名和值（如：NODE_ENV=production）

6. **生成并下载**

### 第三步：使用生成的Dockerfile

1. **保存文件**
   - 将Dockerfile复制到项目根目录
   - 将.dockerignore复制到项目根目录

2. **构建镜像**
   ```bash
   docker build -t myapp .
   ```

3. **运行容器**
   ```bash
   docker run -p 8080:5000 myapp
   ```

## 🎯 预设模板说明

| 模板 | 何时使用 | 端口 |
|------|---------|------|
| 🐍 Python Flask | Python Web API | 5000 |
| 🐍 Python Django | Python大型Web应用 | 8000 |
| 🟢 Node.js Express | Node.js后端API | 3000 |
| 🟢 Node.js Next.js | React全栈应用 | 3000 |
| ☕ Java Spring | Java企业级应用 | 8080 |
| 🔷 Go Gin | Go Web服务 | 8080 |
| 🐘 PHP Laravel | PHP Web应用 | 80 |
| 🌐 Nginx静态网站 | 纯前端项目 | 80 |
| ⚛️ React + Vite | React SPA | 5173 |
| 💚 Vue + Vite | Vue SPA | 5173 |

## 💡 最佳实践

### 1. 选择合适的镜像
✅ **推荐**：`python:3.11-slim`、`node:18-alpine`
❌ **避免**：`python:3.11`、`node:18`（体积太大）

### 2. 使用.dockerignore
排除不需要的文件：
- `node_modules`
- `__pycache__`
- `.git`
- `*.log`
- `.env`

### 3. 优化构建速度
- 先复制依赖文件，再复制代码
- 利用Docker层缓存

### 4. 安全建议
- 不在Dockerfile中硬编码敏感信息
- 使用环境变量
- 定期更新基础镜像

## 🔧 高级功能

### 健康检查
在"高级选项"中设置：
```
curl -f http://localhost:5000/health
```

### 多阶段构建
对于编译型语言，可以在生成的Dockerfile基础上添加：
```dockerfile
# 构建阶段
FROM golang:1.21 AS builder
WORKDIR /build
COPY . .
RUN go build -o app

# 运行阶段
FROM alpine:latest
COPY --from=builder /build/app /app
CMD ["/app"]
```

### 多个端口
如果需要暴露多个端口：
```dockerfile
EXPOSE 5000 8080
```

## ❓ 常见问题

### Q: 端口冲突怎么办？
A: 修改配置中的端口号，或运行时映射到其他端口：
```bash
docker run -p 8080:5000 myapp
```

### Q: 依赖安装失败？
A: 检查requirements.txt或package.json是否存在且格式正确

### Q: 容器启动后立即退出？
A: 查看日志：`docker logs <container_id>`
可能原因：
- 启动命令错误
- 端口被占用
- 代码有错误

### Q: 如何查看生成的Dockerfile？
A: 生成的Dockerfile在右侧预览区，支持一键复制

## 📚 进一步学习

- [Docker官方文档](https://docs.docker.com/)
- [Dockerfile最佳实践](https://docs.docker.com/develop/develop-images/dockerfile_best-practices/)
- [Docker Hub镜像仓库](https://hub.docker.com/)

## 🆘 获取帮助

遇到问题？
1. 查看上方"使用技巧"区域
2. 查阅README.md文档
3. 检查错误日志

## 🎉 成功案例

使用此工具生成的Dockerfile已成功用于：
- ✅ Flask REST API
- ✅ Django CMS系统
- ✅ Express + React全栈应用
- ✅ Spring Boot微服务
- ✅ Vue/React前端项目
- ✅ Go Web服务

---

**祝你Docker化顺利！🚀**
